fastapi==0.115.5
uvicorn[standard]==0.32.1
pymilvus==2.4.9
openai==1.58.1
sentence-transformers==3.3.1
python-dotenv==1.0.1
pydantic[email]>=2.10.3
httpx==0.27.2
python-multipart==0.0.12
# pymilvus 2.4 still imports pkg_resources; setuptools >=81 removed it,
# so pin below 81 until pymilvus drops the legacy import.
setuptools>=69.0,<81
wheel>=0.42

# environs 9.x (transitive via pymilvus 2.4) reads marshmallow.__version_info__
# which was removed in marshmallow 4. Pin <4 until pymilvus upgrades environs.
marshmallow<4

# Document ingestion (Super-Admin upload pipeline)
pypdf>=4.0
python-docx>=1.1
python-pptx>=1.0
